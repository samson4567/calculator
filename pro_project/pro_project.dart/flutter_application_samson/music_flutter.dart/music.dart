import 'package:flutter/material.dart';
import 'package:flutter_application_samson/main.dart';

void man() {
  runApp(MyApp());

}
class MyMusic extends StatelessWidget {
  const MyMusic({Key? key}) : super(key: key);

  @override
  Widget build (BuildContext context) { MaterialApp(home: Scaffold),
    
  )
    
  }
}